# Nne â€“ Repository structure and optimization

## Current structure (as of review)

```
Nne--Maternal-Health-Assistant/
â”œâ”€â”€ .env                          # HF token etc. (gitignored)
â”œâ”€â”€ .gitignore
â”œâ”€â”€ README.md
â”œâ”€â”€ requirements.txt
â”œâ”€â”€ nne_chatbot_app.py            # Gradio chatbot app (entry point)
â”œâ”€â”€ nne-llm-fine-tuning-pipeline.ipynb
â”œâ”€â”€ Nne_Maternal_Health_Chatbot_Report.txt
â”‚
â”œâ”€â”€ data/
â”‚   â””â”€â”€ dataset/
â”‚       â”œâ”€â”€ README.md
â”‚       â””â”€â”€ nne_mixed_train_v2.jsonl
â”‚
â”œâ”€â”€ docs/
â”‚   â”œâ”€â”€ REPO_STRUCTURE.md         # this file
â”‚   â””â”€â”€ images/
â”‚       â”œâ”€â”€ README.md
â”‚       â”œâ”€â”€ Greeting.png
â”‚       â”œâ”€â”€ Clinical Question.png
â”‚       â”œâ”€â”€ Medical Question.png
â”‚       â””â”€â”€ Redirection.png
â”‚
â”œâ”€â”€ experiment_results/           # Evaluation results per experiment
â”‚   â”œâ”€â”€ exp1_baseline/
â”‚   â”‚   â”œâ”€â”€ experiment_results.json
â”‚   â”‚   â””â”€â”€ detailed_evaluation.csv
â”‚   â”œâ”€â”€ exp2_high_lr/
â”‚   â”œâ”€â”€ exp3_more_epochs/
â”‚   â””â”€â”€ exp4_one_epoch/
â”‚
â”œâ”€â”€ outputs_nne/                  # Training outputs; checkpoints ignored via .gitignore
â”‚   â””â”€â”€ nne_exp2_high_lr/
â”‚       â”œâ”€â”€ adapter_config.json, tokenizer*.json, etc.
â”‚       â””â”€â”€ checkpoint-*/         # (ignored)
â”‚
â””â”€â”€ scripts/
    â””â”€â”€ modal_jupyter.py          # Modal Cloud Jupyter launcher
```

---

## Optimal structure (recommended)

Use this as a target layout. Renames and moves are optional but improve clarity and tooling.

```
Nne--Maternal-Health-Assistant/
â”œâ”€â”€ .env
â”œâ”€â”€ .env.example                 # NEW: template for HF_TOKEN (no real secrets)
â”œâ”€â”€ .gitignore
â”œâ”€â”€ README.md
â”œâ”€â”€ requirements.txt
â”‚
â”œâ”€â”€ nne_chatbot_app.py           # Main app entry point
â”œâ”€â”€ nne-llm-fine-tuning-pipeline.ipynb
â”‚
â”œâ”€â”€ data/
â”‚   â””â”€â”€ dataset/
â”‚       â””â”€â”€ nne_mixed_train_v2.jsonl
â”‚       â””â”€â”€ README.md            # NEW: describe dataset, license, how to get it
â”‚
â”œâ”€â”€ docs/
â”‚   â”œâ”€â”€ REPO_STRUCTURE.md
â”‚   â”œâ”€â”€ images/
â”‚   â”‚   â”œâ”€â”€ README.md
â”‚   â”‚   â””â”€â”€ *.png
â”‚   â””â”€â”€ report/                  # OPTIONAL: move report here
â”‚       â””â”€â”€ Nne_Maternal_Health_Chatbot_Report.txt
â”‚
â”œâ”€â”€ experiment_results/          # RENAME from "Experiment Results" (no spaces)
â”‚   â”œâ”€â”€ exp1_baseline/
â”‚   â”œâ”€â”€ exp2_high_lr/
â”‚   â”œâ”€â”€ exp3_more_epochs/
â”‚   â””â”€â”€ exp4_one_epoch/
â”‚
â”œâ”€â”€ outputs_nne/                 # Keep; ensure .gitignore excludes large artifacts
â”‚   â””â”€â”€ nne_exp2_high_lr/
â”‚
â””â”€â”€ scripts/                     # OPTIONAL: dev/automation
    â””â”€â”€ modal_jupyter.py         # if you move it
```

---

## Optimization points (actionable)

### 1. Naming and paths

| Issue | Recommendation |
|-------|----------------|
| **`Experiment Results`** has a space | Rename to `experiment_results` (or `experiments`). Avoids CLI and script issues (`cd "Experiment Results"` vs `cd experiment_results`). |
| **Image filenames with spaces** | Optional: rename to `Clinical-Question.png`, `Medical-Question.png` so URLs don't need `%20`. Not required if current links work for you. |

If you rename `Experiment Results` â†’ `experiment_results`, update any paths in the notebook and in the README (e.g. â€œExperiment Tableâ€ or â€œOptimal Configurationâ€ sections) that refer to that folder.

---

### 2. .gitignore and large artifacts

| Item | Recommendation |
|------|----------------|
| **`outputs_nne/`** | âœ… `outputs_nne/*.zip` and `outputs_nne/*/checkpoint-*/` are ignored. Adapter at `outputs_nne/nne_exp2_high_lr/` can still be committed if desired; checkpoints are excluded. |
| **`.env`** | Already ignored. Keep it that way. |
| **Data** | If `nne_mixed_train_v2.jsonl` is large or private, consider adding `data/dataset/*.jsonl` to `.gitignore` and documenting in `data/dataset/README.md` how to obtain or generate the file. |

---

### 3. Documentation and onboarding

| Addition | Purpose |
|----------|--------|
| **`.env.example`** | Add a file with `HF_TOKEN=your_token_here` (or empty) so contributors know which env vars are needed. |
| **`data/dataset/README.md`** | Short note: what the dataset is, where it comes from, how to get it (or that itâ€™s generated by the notebook), and any license/usage notes. |
| **`docs/report/`** | Optional: move `Nne_Maternal_Health_Chatbot_Report.txt` to `docs/report/` so the repo root stays minimal. Update README â€œReportâ€ link if you add one. |

---

### 4. Entry points and scripts

| Current | Recommendation |
|---------|----------------|
| **`nne_chatbot_app.py` at root** | Keep. Standard for a single-app repo and matches README â€œRun: python app.pyâ€. |
| **`scripts/`** | Contains `modal_jupyter.py` (Modal Cloud Jupyter launcher). |

---

### 5. What's already in good shape

- **Root**: README, requirements, single app entry (`nne_chatbot_app.py`), one main notebook.
- **`data/dataset/`**: Single canonical dataset path and README; notebook and main README point to one file.
- **`docs/images/`**: Screenshots and README; README references are correct.
- **`experiment_results/`**: Clear per-experiment folders with JSON + CSV (no spaces in name).

---

## Quick checklist

- [x] Rename `Experiment Results` â†’ `experiment_results`.
- [x] Add `.env.example` with `HF_TOKEN=` (and any other required vars).
- [x] Tighten `.gitignore` for `outputs_nne/*/checkpoint-*/`.
- [x] Add `data/dataset/README.md` describing the dataset.
- [x] README and app docstring use `python nne_chatbot_app.py`.
- [ ] Optionally move `Nne_Maternal_Health_Chatbot_Report.txt` to `docs/report/` and fix links.

After these, the repo will be well-structured for collaboration, CI, and clear separation of code, data, docs, and experiment outputs.
